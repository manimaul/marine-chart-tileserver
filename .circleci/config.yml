version: 2
jobs:
  build:
    docker:
    - image: manimaul/proxygen-builder:stretch
    steps:
    - checkout
    - run:
        name: GTests
        command: |
          git submodule update --init --recursive
          mkdir build
          cd build
          cmake ..
          make install
          /opt/mct/bin/runUnitTests
